summary: Integration tests for libaio1t64

execute: |
  rootfs="$(install-slices libaio1t64_libs)"

  apt install -y gcc
  arch=$(gcc -dumpmachine)

  test -f ${rootfs}/usr/lib/${arch}/libaio.so.1t64
